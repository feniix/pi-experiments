# Test pi-text-utils: text transform

Use the `text_transform` tool to slugify this text:

> Hello, Portable pi + MCP Tools!

After the tool call, report only the transformed output and one sentence confirming which tool was used.
