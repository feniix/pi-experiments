# Test pi-text-utils: combined flow

Use `text_transform` to uppercase this text:

> portable tool adapters

Then use `text_stats` on the transformed text. Finish with the transformed text and the stats returned by the tools.
