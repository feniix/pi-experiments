# Test pi-text-utils: text stats

Use the `text_stats` tool to count characters, words, and lines in this text:

```text
one two
three four five
```

After the tool call, summarize the counts in a compact bullet list.
